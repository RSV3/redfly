.row
	{{partial "sorting"}}
	+unless ('theResults')
		.myloading
			+unless ('empty')
				i.fa.fa-spinner.fa-spin
			+if ('empty')
				span.empty	No results match filter
	+if ('theResults')
		{{partial "paging"}}
		{{partial "results"}}
		{{partial "paging"}}
