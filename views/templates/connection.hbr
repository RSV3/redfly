{{#unless connected}}
	<p class="alert">
		{{#if canConnect}}
			Offline 
			{{#unless hideReconnect}}
				&ndash; <a {{action connect target="view"}}>Reconnect</a>
			{{/unless}}
		{{else}}
			Unable to reconnect &ndash; <a {{action reload target="view"}}>Reload</a>
		{{/if}}
	</p>
{{/unless}}
