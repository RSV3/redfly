| <a href="#" {{action toggle target="view"}}>
i.icon-pencil.icon-large
| </a>
| {{#if view.show}}
.popover
	| {{#if view.nothing}}
	.alert.alert-error Dudebro, you have to enter something dude, bro.
	| {{/if}}
	| {{#if view.duplicate}}
	.alert.alert-info
		| Another contact has the {{view.primaryAttribute}} "{{view.duplicate}}".
		br
		a(href='#') Merge duplicate
		|  (coming soon).
	| {{/if}}
	.clearfix
		.pull-right
			| {{#unless view.working}}
			| <a href="#" {{action save target="view"}}>
			| Save 
			i.icon-ok
			| </a>
			| <a href="#" {{action toggle target="view"}}>
			| &nbsp;
			| Close 
			i.icon-remove
			| </a>
			| {{else}}
			| working...
			| {{/unless}}
	.nav-header {{view.prettyPrimaryAttribute}}
	| {{view Ember.TextField valueBinding="view.primary"}}
	| {{#if view.others.length}}
	.nav-header {{view.prettyOtherAttribute}}
	| {{#each other in view.others}}
	| {{#view view.itemView otherBinding="other"}}
	| {{view Ember.TextField valueBinding="other.content"}}
	.pull-right
		| <a href="#" {{action promote target="view"}}>
		i.icon-arrow-up
		| </a>
		| <a href="#" {{action remove target="view"}}>
		i.icon-trash
		| </a>
	| {{/view}}
	| {{/each}}
	| {{/if}}
	| {{#if view.primary}}
	div
		| <a href="#" {{action add target="view"}}>
		i.icon-plus
		|  Add another
		| </a>
	| {{/if}}
| {{/if}}
