<div class="row-fluid">
	<h1 class="name">{{name}}</h1>
	<p>Signed up on {{date date}}</p>
	<br>
	<p>You&apos;ve added {{total}} contact{{#unless_eq total compare=1}}s{{/unless_eq}} so far.</p>
	<ul>
		{{#each contacts}}
			<li><a {{action goContact this href=true}}>{{name}}</a></li>
		{{/each}}
	</ul>
	<br>
	<p><em>...more metrics to come!</em></p>
</div>
